// import user from '../data/user.json' assert {type: "json"}

// export const findUserById = (id) => {
//     const users = user.find(u => u.id === id)
//     return users ? users : 'USER NOT FOUND'

// }

// export default { findUserById }
